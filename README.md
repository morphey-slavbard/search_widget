# search_widget
Little widget to show the search percentages for VIT and TXT, also highlights the product number it is pulling from.

Add to requestly and then target it to after page loads, along with /r/experience-search as the targeting.
